import { describe, it } from 'vitest'

describe.sequential('run', () => {
  it('should be generate test files next to the test subject files and then can be run those tests', () => {
  })

  it('should be generate test files to `ct` directory and then can be run those tests', () => {
  })
})
